## Packages
framer-motion | Page transitions and scroll-triggered animations

## Notes
- Using `/media/*` absolute paths for video and images as requested
- Contact form submits to `POST /api/inquiries`
